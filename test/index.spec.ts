/// <reference path="../typings/main.d.ts"/>

import updver from "../lib/index";
import {expect} from "chai";


describe("updver", () => {
  it("should update version numbers", async() => {
    expect(true).to.equal(true);

  });
});

